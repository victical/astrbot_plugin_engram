# 代码审查报告 — 完整版（全维度扫描）

**审查日期**：2026-05-17
**审查范围**：`E:\AI\shouban\astrbot_plugin_engram` 全部 Python 源码（11747 行，不含 `tests/`、`docs/`、`__pycache__/`）
**审查方法**：7 个并行 Agent 横向扫描 + 人工抽样验证
**审查维度**：
1. 未定义方法 / NameError
2. dict 引用污染 / 副作用
3. SQL / format 注入
4. 资源泄漏 / 并发安全
5. 异常处理 / 静默吞错
6. 边界值 / None / 类型混用
7. WebUI 端点安全
8. 调度 / 时序 / 时区
9. 插件生命周期 / 配置热更新

---

## 总览（按严重程度）

| 严重程度 | 数量 | 含义 |
|---|---|---|
| 🔴 Critical (P0) | 11 | 运行时崩溃、数据丢失、数据完整性破坏、接口 100% 失败 |
| 🟠 High (P1) | 14 | 功能降级、静默失败、资源泄漏、并发竞争 |
| 🟡 Medium (P2) | 13 | 潜在副作用、信息泄漏、性能问题、配置失效 |
| 🟢 Info (P3) | 4+ | 防御加固、风格问题 |

---

# 🔴 Critical (P0) — 必须立即修复

## C1. `_format_export_output` 方法未定义

**位置**：`core/memory_manager.py:2992`
**触发**：`export_all_users_messages` 当数据库无符合时间范围的消息时
**代码**：
```python
if not raw_msgs:
    return self._format_export_output(format, [], {})  # 方法不存在
```
**修复**：实现该方法，或简化为 `return True, "", {"exported": 0}`。

---

## C2. WebUI `/api/stats` 双重 NameError

**位置**：`webui_server.py:1077` + `webui_server.py:1079`
**触发**：每次调用 `/api/stats` 或 `/api/stat` 别名
**代码**：
```python
usry_params.get("user_id")                                  # NameError 1
stats = await self._collect_stats(self.db, user_id=user_id) # NameError 2
```
**修复**：
```python
user_id = (request.query_params.get("user_id") or "").strip() or None
```

---

## C3. `_merge_profile_meta` 原地写脏 `current_persona` → rollback 失效

**位置**：`core/profile_manager.py:329-360` + `519-530`
**触发**：每次画像更新
**问题**：`_merge_profile_meta` 直接 mutate 入参 `old_meta["fields"]`（与 `current_persona["_meta"]["fields"]` 同一引用），紧接着 `_snapshot_profile(user_id, current_persona)` 保存的"旧版本"已被本次更新污染。**用户日后回滚拿不回真正的旧状态**。
**修复**：入口 `copy.deepcopy(old_meta)`，或调换 snapshot/merge 顺序。

---

## C4. 失败项被错误标记为成功 → 向量永久丢失

**位置**：`core/memory_manager.py:651, 664`
**触发**：向量补偿队列处理失败时
**代码**：
```python
except Exception as e:
    if retry_count < max_retry:
        failed_payload.append({...})
    success_ids.append(index_id)   # ← 把失败的 id 当成功！
```
**影响**：补偿队列错把失败项标记完成，**这条向量永远写不进 Chroma 但又不再重试**，记忆永久失踪且无审计日志。
**修复**：失败分支不应 `append(index_id)` 到 `success_ids`。

---

## C5. `summarize_prompt` 未配置时崩溃

**位置**：`core/memory_manager.py:1296`
**触发**：用户没在 config 配 `summarize_prompt`
**代码**：
```python
custom_prompt = self.config.get("summarize_prompt")   # 返回 None
prompt = custom_prompt.replace("{{chat_text}}", ...)  # AttributeError: NoneType
```
**修复**：`custom_prompt = self.config.get("summarize_prompt") or DEFAULT_SUMMARIZE_PROMPT`。

---

## C6. LLM 空响应导致画像更新崩溃

**位置**：`core/profile_manager.py:501-506`
**触发**：provider 因超时/限流返回 `completion_text=None`
**代码**：
```python
content = resp.completion_text   # 可能 None
if "```json" in content:         # TypeError: argument of type 'NoneType'
```
对照：`memory_manager.py:1317` 已有 `if full_content and ...` 守护，profile_manager 漏了。
**修复**：`content = (resp.completion_text or "").strip()`。

---

## C7. OneBot 同步：非数字字段直接 `int()` 崩溃

**位置**：`handlers/onebot_sync.py:146, 173-174, 180-181`
**触发**：OneBot 上游返回非数字字符串（空串、含字母）
**代码**：
```python
basic_info["job"] = self.utils.get_career(int(career_id))    # 行 146
basic_info["constellation"] = self.utils.get_constellation(int(b_month), int(b_day))
```
**影响**：整个 OneBot 资料同步流程崩溃。
**修复**：包 try/except 或先用正则校验 `if career_id.isdigit():`。

---

## C8. 配置类型混用：`"7" > 0` TypeError

**位置**：`core/memory_manager.py:1086-1088`
**触发**：用户在 config 写字符串值 `"max_history_days": "7"`
**代码**：
```python
max_days = self.config.get("max_history_days", 0)   # 可能字符串
if max_days > 0:                                    # Py3 TypeError
    cutoff_date = (datetime.datetime.now() - datetime.timedelta(days=max_days)).date()
```
**修复**：强制 `max_days = int(self.config.get("max_history_days", 0) or 0)`。

同模式还有：`scheduler.py:759, 764` `decay_rate <= 0`。

---

## C9. `rebuild_vector_collection` 无锁替换 `self.collection`

**位置**：`core/memory_manager.py:2836-2839`
**触发**：管理员执行 `/重建记忆向量 full` 时有用户并发发消息
**问题**：`delete_collection` → `self.collection = get_or_create_collection(...)` 重新赋值，**期间 `record_message` / `_summarize_private_chat` 仍通过 `self.collection.add(...)` 读旧引用**，命中"集合已删但缓存的旧 collection 引用"窗口。
**修复**：用 `_chroma_init_lock` 覆盖 rebuild 路径，或读写分离。

---

## C10. ThreadPoolExecutor 提前 shutdown 导致 SQLite 半提交

**位置**：`main.py:1287`
**触发**：插件 terminate 时有归档/折叠任务在执行
**代码**：
```python
# 给调度任务 0.5s wait_for（L1251）
self.logic.executor.shutdown(wait=False)   # ← 立即关闭，不等 worker 完成
```
**影响**：worker 线程内的 `self.db.save_memory_index(...)` 尚未提交即被切断，留下半挂起事务和 WAL 残留。
**修复**：`shutdown(wait=True, cancel_futures=False)` + 增加 terminate 超时到 5s。

---

## C11. WebUI POST/DELETE 端点 query 参数类型崩溃

**位置**：`webui_server.py:551-552, 803-804, 875, 1118-1121, 1226-1227`
**触发**：客户端传 `?page=abc` 或 `?limit=xxx`
**代码**：
```python
page = int(query.get("page", 1))           # 行 551，无 try
limit = request.query_params.get("limit", 8)  # 行 1118，未 int()，下游切片用 int 类型时 TypeError
```
**修复**：统一封装 `_parse_int(value, default, min_v, max_v)`。

---

# 🟠 High (P1) — 本周内修复

## H1. `_LLM_INTENT_PROMPT.format(query=text)` 用户 `{` 注入

**位置**：`services/intent_classifier.py:244`
**影响**：用户消息含 `{`/`{x}` → `KeyError`/`ValueError`，被外层 try/except 吞掉，意图分类静默降级为关键词检测。
**修复**：改用项目其他模板一致的 `replace("{{query}}", text)` 模式。

---

## H2. `json.loads(ref_uuids)` 多处无 try-catch

**位置**：`core/memory_manager.py:2469, 2517, 2540, 2606`
**触发**：DB 存量数据中 `ref_uuids` 字段损坏
**对比**：同文件 `1804, 2318, 2670` 已做 `(TypeError, ValueError)` 容错，删除路径却"裸读"。
**影响**：单条脏数据触发 `JSONDecodeError` → 整个 `delete_memory_by_*` 崩溃，用户既看不到也删不掉那条记忆。
**修复**：抽 `_safe_load_uuids()` helper 统一容错。

---

## H3. `asyncio.create_task` 无 done_callback / 无引用

**位置**：`main.py:87, 102, 202`、`webui_server.py:81`、`core/scheduler.py:43-63`
**问题**：所有调度/WebUI/scheduler 启动的 task 既无 `await`、也无 `add_done_callback`、也无 task ref（main.py:87/102/202 直接丢弃返回值）。
**影响**：
- 调度器循环内若抛非 CancelledError，**整个后台归档/折叠/向量补偿静默死亡**，用户无感知；
- Python 弱引用 GC 可能回收悬空 task。

**修复**：保留 `self._tasks = [asyncio.create_task(...)]`，并 `task.add_done_callback(_log_task_failure)`。

---

## H4. `asyncio.Lock` 在同步 `__init__` 中创建

**位置**：`core/memory_manager.py:117, 137`、`main.py:83`、`services/friend_cache.py:19`、`webui_server.py:54, 57`
**问题**：Python <3.10 中 `asyncio.Lock()` 会绑定到 `get_event_loop()`，若插件实例化时 loop 尚未运行或不是同一 loop，运行期会抛 `RuntimeError: ... is bound to a different event loop`。
**修复**：lazy 初始化（首次 acquire 时再创建），或要求 Python ≥ 3.10。

---

## H5. `background_worker` 异常路径无退避 sleep

**位置**：`core/scheduler.py:130-172`
**问题**：循环内 `await self.logic.check_and_summarize()` 抛异常后**仅打日志不 sleep**——其他 scheduler（249/443/489/536/745）都有 60s 退避，唯独这个漏了。
**影响**：持续秒级失败时进入紧密死循环刷日志，**CPU 100%**。
**修复**：在 except 后加 `await asyncio.sleep(60)`。

---

## H6. WebUI 渲染 ClientSession 泄漏

**位置**：`webui_server.py:1146-1170` (`/api/profile/{user_id}/render`)
**问题**：每次请求新建 `ProfileRenderer` → 创建 `aiohttp.ClientSession`，但 `renderer.close()` 在 try 块尾部，**渲染失败/超时时 close 跳过**。
**影响**：每个失败请求泄漏一个 ClientSession + TCP connector。
**修复**：`try/finally` 或 `async with`。

---

## H7. 插件 reload 时旧实例残留 → 双调度 + 端口冲突

**位置**：`main.py:87, 102, 202`、`webui_server.py:107-108`
**问题**：
- AstrBot `reload` 触发 `__init__` 重建实例，但 `terminate()` 中 `await self._server_task` 无超时，uvicorn 持有 keep-alive 连接时**卡死**；
- scheduler 任务 cancel 后只等 0.5s（main.py:1251），未结束即放弃；
- 新实例已 `create_task` → **两套 worker 并存**，WebUI 端口冲突。

**修复**：
- `await asyncio.wait_for(self._server_task, timeout=3.0)`；
- scheduler.start() 加幂等保护（检查 `self._started`）；
- terminate 超时改为可配置。

---

## H8. 删除流程异常后状态污染（重复插入 delete_history）

**位置**：`core/memory_manager.py:2598, 2755-2760`
**问题**：
```python
self._delete_history[user_id].insert(0, delete_record)   # 第 2598 行已经插入
# 之后 chroma.delete / db.delete_memory_index 可能抛错
except Exception as e:
    if not history_id:
        self._delete_history.setdefault(user_id, []).insert(0, delete_record)  # 再次插入
```
**影响**：DB 已删但 Chroma 未删时，热缓存出现重复记录；后续 undo 会拿到错位记录。
**修复**：成功后再插入 history，或在 except 内先检查是否已 insert。

---

## H9. 群聊跨数据泄漏风险

**位置**：`main.py:170-188`
**问题**：所有群共用一份 SQLite (`engram_memories_group.db`) 和一个 Chroma collection，仅靠 `user_id`/`group_id` 字段过滤。
**影响**：任何向量召回或 SQL 查询条件遗漏 `group_id` 过滤都会导致 A 群看到 B 群的记忆。需逐个核对调用点。
**修复**：要么按 group_id 分库，要么强制全部查询路径都过 `group_id` 过滤包装。

---

## H10. peewee Lock + 多 worker SQLite 句柄泄漏

**位置**：`db_manager.py:153-162`
**问题**：`SqliteExtDatabase` 默认 thread-local 连接；每个 executor worker 首次 `connection_context()` 建立连接但 `db.close()` 只关当前线程的句柄。
**影响**：4 个 worker 各持一份永不释放的 SQLite 句柄，WAL 文件无法 checkpoint；terminate 时未调用 `db.close()`，Windows 文件锁残留。
**修复**：terminate 时显式 `for db in [...]: db.close()` 在每个 worker 中执行。

---

## H11. `update_persona` 解析空响应崩溃 / scheduler dict 迭代竞争

**位置**：`core/memory_manager.py:121-122 + 985-986 + 2126` 配 `core/scheduler.py:174-197`
**问题**：`last_chat_time` / `unsaved_msg_count` 跨线程读写无锁。`_cleanup_inactive_users` 在 `check_and_summarize` 内 `for user_id in self.last_chat_time.items(): pop(...)`，executor 线程的 `clear_user_data` 回调也直接写。
**影响**：可能 `RuntimeError: dictionary changed size during iteration`。
**修复**：迭代前 `list(self.last_chat_time.items())` 快照，写入用 asyncio.Lock。

---

## H12. `int()` 强转无 try 影响画像更新

**位置**：`services/profile_guardian.py:290, 473`
**代码**：
```python
prop["confidence"] = int(prop.get("confidence", 0)) + 1
exist["confidence"] = max(int(exist.get("confidence", 0)), int(p.get("confidence", 0)))
```
**触发**：proposals 历史数据若是 `"confidence": "high"` 字符串
**影响**：`int("high")` ValueError → 整个画像更新失败。

---

## H13. WebUI 命令 handler 全无 try/except

**位置**：`main.py:862-987`
**影响**：handler 内部异常未捕获时冒泡到 AstrBot 全局错误处理，可能让整个事件链断。
**修复**：所有命令 handler 统一包 try/except 并返回友好提示。

---

## H14. profile 文件写入无原子性

**位置**：`core/profile_manager.py:169, 230, 270, 314, 531`
**代码**：`with open(path, 'w') as f: json.dump(profile, f, ...)`
**影响**：写入过程中崩溃/被杀，留下空文件或部分写入；下次 `json.load` 失败被吞，**静默丢失用户画像**。
**修复**：写入 `path.tmp` 后 `os.replace(path.tmp, path)`。

---

# 🟡 Medium (P2) — 排期修复

## M1. `ProfileGuardian.validate_update` 原地修改入参 `new_profile`

**位置**：`services/profile_guardian.py:132-135`
**问题**：`new_social = new_profile.get("social_graph", {})` 返回引用而非副本，后续 `new_social["interaction_stats"] = old_stats` 污染调用方 `new_profile`。
**当前**：调用方不再读 `proposal`，未引发用户可见 bug，但破坏函数契约。
**修复**：`new_social = dict(new_profile.get("social_graph", {}))`。

---

## M2. WebUI 错误响应泄漏内部信息

**位置**：`webui_server.py:512, 540, 599, 664, 734, 773, 793, 859, 928, 1013, 1072, 1083, 1108, 1127, 1137, 1185, 1208, 1218, 1236`
**代码**：`return {"error": str(exc)}`
**影响**：直接把 SQL 错误信息、文件绝对路径、peewee 内部细节回传客户端。
**修复**：服务端日志详写，客户端只返 `"内部错误"` + 错误 ID。

---

## M3. CORS 配置失效

**位置**：`webui_server.py:432-438`
**问题**：`allow_origins=[f"http://{self.host}:{self.port}", "http://localhost", "http://127.0.0.1"]`
- 当 `host="0.0.0.0"` 时 origin 变成 `http://0.0.0.0:8080`（无效）；
- `localhost`/`127.0.0.1` **缺端口号**，浏览器实际发送的 origin 是 `http://localhost:8080`。
- 结果：**CORS 实际全部失效**，前端跨端口访问被拒。

**修复**：
```python
allow_origins=[f"http://localhost:{self.port}", f"http://127.0.0.1:{self.port}"]
```

---

## M4. POST body 无大小限制

**位置**：`webui_server.py:454-499` 所有 POST 路由
**影响**：FastAPI 默认无 body 大小限制，攻击者发巨型 JSON 触发内存膨胀（DoS）。
**修复**：依赖项加 `Body(..., max_length=64*1024)` 或全局 middleware 限制。

---

## M5. datetime 全部用 `now()` 无时区

**位置**：`core/scheduler.py:206-209, 280-284, 397-403, 453, 500, 710-712` + 全项目其他位置
**影响**：DST 切换日（夏令时）sleep 偏差 ±1 小时；跨时区部署 cron 语义不明。
**修复**：用 `datetime.now(tz=ZoneInfo("Asia/Shanghai"))` 或文档化"本机时区"。

---

## M6. 配置固化：改了不生效

**位置**：
- `services/intent_classifier.py:70` `_mode`、`_pattern_mode`、`_min_length`
- `services/friend_cache.py:20` `_refresh_interval`
- `core/memory_manager.py:131` `_embedding_provider_id`
- `core/scheduler.py:54-64, 392-393` 调度参数
- `main.py:90, 92` `webui_port`/`webui_host`/`enable_webui_server`

**问题**：所有 service 在 `__init__` 把 config 拍照成实例属性，运行时改 config 不感知。
**修复**：在每次 hot path 读取时重新 `self.config.get(...)`。

---

## M7. 调度器无 last_run_at 持久化

**位置**：`core/scheduler.py:267-315`（daily_persona）+ weekly/monthly/yearly
**问题**：每日 00:00 触发后若崩溃/reload，重启后**当天又会完整跑一遍**，画像被重复更新。
**修复**：写 `last_run_at` 到 `plugin_data_dir/scheduler_state.json`，重启时跳过已执行的窗口。

---

## M8. `_calculate_next_check_time` 跨线程无锁读 dict

**位置**：`core/scheduler.py:174-197`
**问题**：迭代 `last_chat_time.items()` 时 `record_message` 并发写入。
**修复**：迭代前快照。

---

## M9. terminate 超时 0.5s 过短

**位置**：`main.py:1249-1252`
**影响**：`_execute_daily_persona_update` 内 LLM 调用 0.5s 远不够，被强切后 DB 半提交。
**修复**：改 5-10s。

---

## M10. 群聊首次消息延迟极高

**位置**：`main.py:158-204`
**问题**：群聊首次消息时同步初始化 `DatabaseManager` + `verify_contract` + `MemoryManager` + `MemoryScheduler` + chromadb，**首条消息延迟数百毫秒至秒级**。
**修复**：在 `__init__` 阶段就 schedule 群聊初始化任务，或后台预热。

---

## M11. `member_id` 分页路径 Python 层全表扫描

**位置**：`webui_server.py:825-842`
**问题**：用户传 `member_id` 过滤时不走 SQL 而是 `list(query)` 后 Python 层 filter，大群组性能爆炸。
**修复**：把 `member_id` 加进 ORM `.where(...)`。

---

## M12. PIL `Image.open` 惰性句柄

**位置**：`profile_renderer.py:149-154`
**问题**：`Image.open(cache_file)` 返回 lazy image，文件句柄在 GC 前不释放；后续 `os.remove(cache_file)` 在 Windows 抛 `PermissionError: file in use`。
**修复**：`with Image.open(cache_file) as img: img.load()` 后再 close 文件。

---

## M13. `delete_history` 双层 except 吞掉 raise

**位置**：`core/scheduler.py:156-158`
**代码**：
```python
except Exception as e:
    self._observe_run(task_name, started_at, False, e)
    raise   # ← 抛出后被外层另一个 except Exception 重新捕获
except asyncio.CancelledError: ...
except Exception as e:   # ← 紧接着同函数下一个 except 接住
    ...
```
**影响**：日志重复打两遍；看起来"已抛出"实际未中断循环。

---

# 🟢 Info (P3) — 防御加固

## I1. FTS5 MATCH 控制字符未过滤

**位置**：`db_manager.py:500-509`
**说明**：审查清单中"SQL 注入"在此处不成立——`match_expr` 经 `?` 参数绑定，双引号 phrase literal 转义有效。但 `.strip()` 仅去首尾空白，NUL 字节可能让 sqlite3 抛 `ProgrammingError`（已被外层 try/except 兜底，无 DoS 实际影响）。
**加固**：
```python
safe = re.sub(r'[\x00-\x1f\x7f"]', ' ', token).replace('"', '""').strip()
if len(safe) > 64:
    safe = safe[:64]
```

## I2. WebUI 全部使用 peewee ORM 参数化，无 raw SQL

无注入面。

## I3. `int(b_month)`/`int(b_day)`/`int(career_id)` 全部应加 try-catch

OneBot 字段格式不可信，需要先验证再转换。

## I4. `friend_cache._refresh_interval` 启动时拍照，改配置无效

与 M6 同类。

---

# 修复优先级与工作量估算

| 优先级 | Bug 编号 | 工作量 | 备注 |
|---|---|---|---|
| 🔴 立即 (今日) | C2, C5, C6 | 各 1-3 行 | 接口崩溃 + 用户配置缺省即崩 |
| 🔴 立即 (今日) | C7, C8, C11 | 各 5-10 行 | OneBot / 配置类型兼容 |
| 🔴 本周一 | C3, C4 | 各 5-15 行 | 数据完整性 |
| 🔴 本周一 | C9, C10 | 各 10-20 行 | 并发 / 资源管理 |
| 🔴 本周一 | C1 | 5 行 | 实现 / 简化 |
| 🟠 本周内 | H1, H2, H4, H6, H8, H14 | 各 1-30 行 | 静默失败 / 资源泄漏 |
| 🟠 下周 | H3, H5, H7, H9, H10, H11, H12, H13 | 较多 | 并发安全 / 资源回收 |
| 🟡 排期 | M1-M13 | 视情况 | 性能 / 安全加固 |
| 🟢 加固 | I1-I4 | 防御性 | 非紧急 |

---

**审查人**：claude-sonnet-4-6（基于 7 个并行 Agent 横向扫描 + 抽样验证）
**生成时间**：2026-05-17
**与上一版差异**：从"原清单 6 项复核"扩展为"全维度 42 项 bug 完整审查"
